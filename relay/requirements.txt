# =============================================================================
# SMTP Relay — relay service dependencies
# Generated: 2026-04-24
# All versions pinned to the latest stable release on PyPI at generation time.
# =============================================================================

# SMTP server (asyncio).
aiosmtpd==1.4.6

# Microsoft Authentication Library for Client Credentials Grant.
msal==1.36.0

# HTTP client used by the Graph wrapper.
httpx==0.28.1

# ORM + async driver.
SQLAlchemy==2.0.49
aiosqlite==0.22.1

# Password hashing (used to verify SMTP account passwords).
bcrypt==5.0.0

# Fernet symmetric encryption for application secrets at rest.
cryptography>=46.0.5

# =============================================================================
# Build dependencies for the native-Windows package.
# Union of the relay + UI runtime requirements plus PyInstaller. Pinned to the
# same versions as relay/requirements.txt and ui/requirements.txt; keep these
# three in sync when bumping a dependency.
# =============================================================================

# --- Web UI (uvicorn serves the panel directly; nginx is dropped on Windows) -
# Plain uvicorn (not [standard]): the panel is plain HTTP on loopback with no
# websockets and no auto-reload, so we don't need watchfiles (a large Rust
# binary), websockets, httptools or PyYAML. uvicorn falls back to its built-in
# h11 HTTP implementation and the asyncio loop, which is all we use.
fastapi==0.136.0
uvicorn==0.44.0
pydantic==2.13.0
pydantic-settings==2.14.2
python-multipart==0.0.31
jinja2==3.1.6
itsdangerous==2.2.0
slowapi==0.1.9
alembic==1.18.4
pyotp==2.9.0
qrcode==8.2

# --- SMTP relay --------------------------------------------------------------
aiosmtpd==1.4.6

# --- Shared ------------------------------------------------------------------
msal==1.36.0
httpx==0.28.1
SQLAlchemy==2.0.49
aiosqlite==0.22.1
bcrypt==5.0.0
cryptography>=46.0.5

# --- System-tray controller --------------------------------------------------
pystray==0.19.5
pillow==12.3.0

# --- Packaging ---------------------------------------------------------------
pyinstaller==6.11.1
